{
  "name": "clickatell",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:allure": "cypress run --record --key <your-record-key> && allure generate allure-results --clean -o allure-report",
    "open:allure": "allure open allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cypress": "^13.13.3"
  },
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.40.2"
  }
}
